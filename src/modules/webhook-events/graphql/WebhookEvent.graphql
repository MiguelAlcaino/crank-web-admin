query WebhookEvent($id: ID!) {
  webhookEvent(id: $id) {
    id
    eventType
    subscriptionId
    status
    attempts
    lastAttemptAt
    nextRetryAt
    createdAt

    parentEventId
    triggeredBy
    resolvedByEventId
    lastError
    destinationUrl
    payload
  }
}
